/** Shared coated-paper finish for original labels and the applied sticker. */
export const CARTRIDGE_LABEL_FINISH = {
  roughness: 0.66,
  metalness: 0,
  clearcoat: 0.24,
  clearcoatRoughness: 0.43,
  envMapIntensity: 1,
  toneMapped: true,
} as const;
