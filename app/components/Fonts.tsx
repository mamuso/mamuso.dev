import localFont from 'next/font/local'

export const monaSans = localFont({
  src: '../../public/fonts/monasans.woff2',
  variable: '--monasans',
})

export const monaspaceNeon = localFont({
  src: '../../public/fonts/monaspace-neon.woff2',
  variable: '--monaspaceneon',
})
